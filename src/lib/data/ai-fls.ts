/**
 * Как запустить Fuzzy Logic Systems и что в ней ведёт себя неожиданно.
 *
 * FLS.exe — 32-разрядное приложение для Windows 1990-х годов, а лабораторная
 * рассчитана на 4–6 аудиторных часов. Больше половины этого времени уходит не
 * на нечёткий вывод, а на то, чтобы программа вообще заработала и не потеряла
 * введённое. Всё, что ниже, проверено запусками, а не взято из документации:
 * документации у программы нет.
 */

import type { LabStep } from "@/components/lab-steps";

export interface RunGuide {
  id: string;
  system: string;
  summary: string;
  /** Проверялся ли этот путь на самом деле. */
  verified: boolean;
  steps: LabStep[];
}

export const RUN_GUIDES: RunGuide[] = [
  {
    id: "windows",
    system: "Windows",
    summary:
      "Ничего ставить не нужно: 64-разрядная Windows запускает 32-разрядные приложения сама. " +
      "Программа не устанавливается — распакуйте архив и запустите exe.",
    verified: false,
    steps: [
      {
        title: "Распаковать архив курса",
        body: "Нужны FLS.exe и папка FLS_Examples с четырьмя готовыми примерами — на них удобно проверить, что всё работает.",
      },
      {
        title: "Запустить FLS.exe",
        body: "Если Windows предупреждает о неизвестном издателе, это ожидаемо: программа не подписана.",
      },
      {
        title: "Открыть пример и убедиться, что вывод считается",
        body: "File → Open → reactor.fls. Три правила, вывод должен дать число, а не −1.",
      },
    ],
  },
  {
    id: "docker",
    system: "Linux и WSL 2 · wine в контейнере",
    summary:
      "Wine с поддержкой i386 живёт в контейнере, основная система остаётся нетронутой: " +
      "добавлять в неё 32-разрядную архитектуру не нужно.",
    verified: true,
    steps: [
      {
        title: "Собрать образ",
        body:
          "Ubuntu с wine, wine32 и xdotool. Архитектура i386 добавляется внутри образа — " +
          "в основной системе ничего не меняется.",
        commands: [
          "# Dockerfile",
          "FROM ubuntu:24.04",
          "RUN dpkg --add-architecture i386 && apt-get update && \\",
          "    apt-get install -y --no-install-recommends wine wine32:i386 \\",
          "    xdotool imagemagick x11-utils xauth fonts-liberation locales && \\",
          "    sed -i 's/^# *ru_RU.UTF-8/ru_RU.UTF-8/' /etc/locale.gen && locale-gen ru_RU.UTF-8",
          "",
          "docker build -t fls-wine .",
        ],
      },
      {
        title: "Поднять контейнер с доступом к экрану",
        body:
          "Под WSL 2 окна выводит WSLg, отдельный X-сервер не нужен — достаточно пробросить " +
          "сокет. Локаль ru_RU.UTF-8 обязательна: без неё wine берёт кодировку, отличную от " +
          "CP1251, и весь интерфейс программы становится нечитаемым.",
        commands: [
          "docker run -d --name flsbox --init --user 1000:1000 \\",
          "  -e HOME=/work -e DISPLAY=:0 -e WINEPREFIX=/wine -e WINEDEBUG=-all \\",
          "  -e LANG=ru_RU.UTF-8 -e LC_ALL=ru_RU.UTF-8 \\",
          "  -v /tmp/.X11-unix:/tmp/.X11-unix -v \"$PWD\":/work -v fls-prefix:/wine \\",
          "  fls-wine sleep infinity",
          "docker exec flsbox wineboot -u",
        ],
      },
      {
        title: "Запустить программу",
        commands: ["docker exec -d flsbox wine /work/FLS.exe"],
      },
    ],
  },
  {
    id: "linux",
    system: "Linux · wine прямо в системе",
    summary:
      "Короче, но требует включить архитектуру i386 в самой системе. Без неё wine32 " +
      "не поставится, а 64-разрядный wine 32-разрядное приложение не запустит.",
    verified: false,
    steps: [
      {
        title: "Включить i386 и поставить wine",
        commands: [
          "sudo dpkg --add-architecture i386",
          "sudo apt update",
          "sudo apt install wine wine32",
        ],
      },
      {
        title: "Проверить разрядность",
        body: "wine должен сообщить, что префикс 32-разрядный либо умеет запускать win32.",
        commands: ["wine --version", "file FLS.exe"],
      },
      {
        title: "Запустить",
        body: "Локаль важна так же, как и в контейнере: иначе интерфейс превращается в мусор.",
        commands: ["LC_ALL=ru_RU.UTF-8 wine FLS.exe"],
      },
    ],
  },
  {
    id: "macos",
    system: "macOS",
    summary:
      "Сложнее всего: начиная с macOS Catalina система сама не запускает 32-разрядный код, " +
      "а на Apple silicon к этому добавляется трансляция архитектуры. Путь не проверялся.",
    verified: false,
    steps: [
      {
        title: "Поставить wine",
        commands: ["brew install --cask --no-quarantine wine-stable"],
      },
      {
        title: "На Apple silicon — включить Rosetta",
        body:
          "Wine для macOS собран под x86_64, поэтому на процессорах Apple он идёт через " +
          "Rosetta, а 32-разрядное приложение — ещё одним слоем внутри wine.",
        commands: ["softwareupdate --install-rosetta --agree-to-license"],
      },
      {
        title: "Запасной путь — виртуальная машина",
        body:
          "Если wine не справляется, надёжнее поставить Windows в UTM или Parallels: " +
          "программа маленькая и работает даже на минимальной конфигурации.",
      },
    ],
  },
];

/**
 * Особенности программы, из-за которых работа не получается с первого раза.
 *
 * Каждая проверена отдельным опытом; там, где опыт можно повторить, сказано как.
 */
export const FLS_PITFALLS: string[] = [
  "Кнопка «Вывод» не пересчитывает степени принадлежности из значений, записанных в файле, а берёт их из текущего состояния программы. После загрузки системы значение каждой входной переменной нужно ввести заново в редакторе переменной, иначе вывод заканчивается результатом −1. Проверяется так: изменить в файле значение входной переменной, не трогая записанные степени, — результат не изменится.",
  "Дизъюнктивная связка посылок (f_or) не восстанавливается при загрузке: все правила помечаются конъюнктивными. Воспроизводится и на штатном примере reactor_2.fls. Переключать приходится в редакторе каждого правила вручную.",
  "После неудачного вывода — результата −1 или пустого поля — главное меню перестаёт отвечать на щелчки. Каждый опыт приходится делать в отдельном запуске программы.",
  "При базе больше пяти правил программа не выдаёт результата, если сработало больше одного правила. Те же входные данные на урезанной до двух правил базе считаются верно; на базах из 6, 9 и 11 правил — нет. Система из трёх правил одновременное срабатывание обрабатывает нормально.",
  "Когда аккумулированное множество становится многоэкстремальным, дефазификация заметно ошибается: в одном опыте программа показала 100,71 вместо расчётных 108,75, в другом не выдала результата вовсе. Для одноэкстремальных множеств результаты сходятся с расчётом до последнего знака.",
  "Имена переменных и термов не могут содержать пробелов. Пробел в имени рвёт разбор строки, и файл открывается частично — без сообщения об ошибке.",
  "Файл читается только в CP1251. Сохранённый в UTF-8, он открывается, но имена термов превращаются в мусор.",
];

/** Строение файла .fls — формат нигде не описан, восстановлен по примерам. */
export const FLS_FORMAT = `fuzzy_logic_system
<число переменных> <имя переменной> <текущее значение>
<левая граница универса> <правая граница> 0 0.95 0.7 1
<число термов> <имя терма>  <левая> <правая> <цвет> <мю>
<число точек> x y x y …          функция принадлежности
<число точек> x y x y …          результат активизации терма
…                                пустая строка после каждой переменной

<методы: девять позиций>
Basic_rule_set <наборов> <правил> IF <n> <переменная> IS <терм> f_and …
THEN <переменная> IS <терм> WITH CERTAINTY <вес>`;

/**
 * Разбор строки методов.
 *
 * Назначение позиций установлено опытом: в программе по очереди менялся один
 * выпадающий список, система сохранялась, файлы сравнивались. Позиции 1 и 4 не
 * изменились ни в одном из опытов.
 */
export const FLS_METHOD_LINE: { position: number; role: string; values: string }[] = [
  { position: 1, role: "не менялась", values: "minimum" },
  { position: 2, role: "конъюнкция", values: "minimum · maximum" },
  { position: 3, role: "дизъюнкция", values: "minimum · maximum" },
  { position: 4, role: "не менялась", values: "minimum" },
  { position: 5, role: "импликация", values: "minimum · maximum" },
  { position: 6, role: "аккумуляция", values: "minimum · maximum" },
  { position: 7, role: "дефазификация", values: "cog · fimax · mom" },
  { position: 8, role: "не менялась", values: "1" },
  { position: 9, role: "не менялась", values: "10" },
];
